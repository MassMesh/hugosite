Using the hugo-icon theme for the moment.

To build the site, run: ../bin/hugo.  The error:

Error: Error copying static files: chmod /home/members/jamesokeefe/sites/massmesh.org/users/massmeshnet/massmesh.org/web/: operation not permitted

does not prevent the site from being installed in the massmesh.org/web folder.
